# ── 服务器配置 ──
PORT=3001

# ── CORS 配置（多个域名用逗号分隔）──
CORS_ORIGIN=http://localhost:5173

# ── Redis 配置（不设置则降级为内存存储）──
# REDIS_URL=redis://localhost:6379
# REDIS_URL=redis://:password@your-redis-host:6379
